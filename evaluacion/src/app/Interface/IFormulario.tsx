export interface Formulario{
    nombre: string,
    apellido: string,
    genero: string | number,
    direccion: string,
    numero: number,
    region: string | number,
    comuna: string | number,
    correo: string,
    telefono: number,
    asunto: string | number,
    contenido: string
}
